# Issue #16 低置信片段选择性二次识别 QA

- 验收日期：2026-07-16
- 分支：`codex/issue-16-selective-retry`
- 基线 HEAD：`ee03e0a731447048bebd929410bd3a26847eb7ed`
- 当前结论：`PRELIMINARY PASS`；提交后仍须由独立 Reviewer 对精确 HEAD 复核并等待 GitHub CI 全绿
- 隐私边界：仅记录不可逆媒体/配置指纹、耗时与数值指标，不记录媒体路径、字幕正文或原始异常

## 验收合同

| 检查项 | 结果 | 证据 |
|---|---|---|
| 保守触发 | PASS | 每个硬触发、弱证据组合、单弱证据与零触发均有纯函数回归 |
| 有界请求 | PASS | 单核心 ≤ 24 秒、单请求 ≤ 28 秒、最多 12 次、总时长受 20%/10–120 秒预算约束，禁止整段二次转写 |
| 自适应上下文 | PASS | 上下文至少前后各 2 秒；真实 120 秒样本在原 20% 总预算内确定性扩展为 24 秒 |
| 单次候选 | PASS | 每个首轮候选最多进入一个请求；未标记候选阻断核心合并 |
| 邻居所有权 | PASS | 跨核心返回按有效词时间戳裁剪；无法安全裁剪即拒绝；两个输出模式均覆盖夹心邻居回归 |
| 严格择优 | PASS | 证据严重度不随分段数量累加；关系型语音空洞不直接进入片段评分。只有内在质量分超过 0.1，或 VAD 覆盖增加超过 30 ms 且内在质量不变差时才采用 |
| 覆盖保真 | PASS | 按区间集合比较而非总采样数；端点容差固定为 30 ms，等总量位置迁移会被拒绝 |
| 程序时间轴 | PASS | 文本一一相同时仅采用二次质量事实，输出继续使用首轮时间与词时间戳 |
| 安全删除 | PASS | 只有核心 VAD 语音采样严格为 0 且二次为空时才删除；任意非零语音均保留首轮 |
| 失败回退与隐私 | PASS | 单请求失败保留首轮，只记录 `failed`，不持久化异常正文 |
| 旧任务兼容 | PASS | 缺少新字段的历史任务迁移为 `selective_retry=false`；新任务默认开启 |

## 真实媒体 A/B

模型/设备固定为本地 `large-v3`、CUDA FP16、Beam Size 5、动态边界、`chunk_segments`。两个变体仅 `selective_retry` 不同：

- 关闭配置指纹：`8f718518217672dc34756b7bac8cc0658d1ab0380a3334e454b559a2816ae79a`
- 开启配置指纹：`dc395c7487f76a6ee8330a9fee0e8aae07e7a47f069fc37d4ba2036fecbc6d1f`

### `real-video-a-120s`

- 媒体 SHA-256：`ef76385e8889457be05319be5221c256e40aae36778a6414df0f0a3f298c62f6`

| 指标 | 关闭 | 开启 | 结论 |
|---|---:|---:|---|
| 最终修复后顺序运行耗时 | 9,125 ms | 10,304 ms | 仅记录观测值，不据此宣称提速 |
| 字幕数 / 非空字符数 | 19 / 133 | 19 / 133 | 无遗漏 |
| 时间轴覆盖 | 34,100 ms | 34,100 ms | 不变 |
| VAD 语音覆盖率 | 0.775111 | 0.775111 | 不变 |
| 低 VAD 覆盖字幕 | 3 | 3 | 未误删真实语音 |
| 精确重复 / overlap | 1 / 0 | 1 / 0 | 未增加重复或重叠 |
| 高 VAD 语音空洞 | 0 | 0 | 未制造新空洞 |
| 重复 ID / 非法范围 | 0 / 0 | 0 / 0 | 稳定 ID 与范围有效 |
| 候选 / 请求 / 采用 / 失败 | 0 / 0 / 0 / 0 | 1 / 1 / 1 / 0 | 只重识别一个可疑区间一次 |
| 二次送模音频 | 0 ms | 24,000 ms | 等于 120 秒媒体的 20% 总预算 |
| 采用结果质量增益 | 0 | 4.264521 | 至少一个质量指标严格改善 |

开启后二次候选使诊断候选总数从 19 变为 20，`deduplicated_segment_count=1` 表示首轮候选被择优替换；最终输出重复数没有增加。二次文本与对应首轮文本一一相同，因此采用更优质量事实的同时保留了首轮程序时间轴。

最终修复后重跑中，Baseline/Candidate 的文本序列指纹均为 `68956ade8953069f7353e0b5f5fd055603f5487d52533470a186aafefef999e6`，时间轴指纹均为 `7637c081255858562fdd6f08ca08611a241c1d0151c2192ee5d79f555f9671d1`。

独立只读 A/B 审计结论为 `PASS`，当时运行时代码指纹为 `5a3a2c60796801b7a4ad5c69c00e16bc2ce340b12abfdc667083499a9a488332`。随后 Reviewer 发现并修复了与该样本无关的 `speech_gap` 评分不对称；修复后的最终 A/B 数值及输出指纹仍保持上述结论：

| 独立复核项 | 结果 |
|---|---|
| 全文序列 SHA-256 | Baseline/Candidate 均为 `f94c27ed61d7578d5eac06003d81deb01df8c7166348f89da6ece2ae58e1a2f6` |
| 目标文本 SHA-256 / 出现次数 | `8059f43b69df9852539afc1f29b566b7f4c42992bf4394d66df37c2398729087` / 两侧均为 1 |
| 首轮 / 模型二次 / 最终时间轴 | 29,850–30,170 / 29,850–30,140 / 29,850–30,170 ms |
| 相邻重复 / 全局重复余量 | 0→0 / 1→1 |
| 时间重叠 | 0 段、0 ms；无新增 |
| VAD 语音覆盖 | 两侧均为 224,224 / 289,280 samples |
| 高 VAD 空洞 / 最大长空洞 VAD 覆盖 | 0→0 / 0.133737→0.133737 |
| ID 唯一连续、越界、倒序 | PASS / 0 / 0 |

审计确认真实短语音未遗漏，二次识别仅改善质量事实，最终时间轴仍由程序持有。

### `real-video-b-20s`

- 媒体 SHA-256：`56bdd85371f6e0c3f4cac7141f87f12f526776f0b3ffad40a39cb83aa5615838`

| 指标 | 关闭 | 开启 | 结论 |
|---|---:|---:|---|
| 最终修复后顺序运行耗时 | 7,330 ms | 7,253 ms | 仅记录观测值 |
| 字幕数 / 非空字符数 | 7 / 51 | 7 / 51 | 不变 |
| 时间轴覆盖 | 8,200 ms | 8,200 ms | 不变 |
| VAD 语音覆盖率 | 0.672965 | 0.672965 | 不变 |
| 精确重复 / overlap | 0 / 0 | 0 / 0 | 不变 |
| 高 VAD 语音空洞 | 0 | 0 | 不变 |
| 重复 ID / 非法范围 | 0 / 0 | 0 / 0 | 不变 |
| 候选 / 请求 / 采用 / 失败 | 0 / 0 / 0 / 0 | 0 / 0 / 0 / 0 | 清晰样本零触发、零二次调用 |

最终修复后重跑中，Baseline/Candidate 的文本序列指纹均为 `132d35859a4cf5185f0f332486befb70e7f465b062c70bf8b4013df16be07ccd`，时间轴指纹均为 `c64aafbbe06261fb26998bdc7d1db7346320809cfdd67223f221e59d18393fdd`。

## Reviewer 修复循环

| 轮次 | 精确 HEAD | Verdict | 处理结果 |
|---|---|---|---|
| 预审 | 未提交工作树 | `CHANGES_REQUIRED` | 修复跨核心邻居重复、分段数量评分偏差、等总覆盖位置迁移三个 P1 |
| 第 1 次精确提交 | `f55fe43e70cf6e4d1564ea9f6ecb31904a23b183` | `CHANGES_REQUIRED` | 修复关系型 `speech_gap` 只存在于首轮诊断而伪造质量增益的 P1 |

最终修复把 `speech_gap` 从片段内在严重度中剥离：空洞未变化且其余事实相同时保留首轮；只有 VAD 语音覆盖增加超过 30 ms 且内在质量不变差，才把覆盖增加视为严格改善。两种输出模式同时覆盖“未填空洞/真实填空洞”Provider 回归。

## 真实浏览器验证

| 检查项 | 桌面 | 390×844 |
|---|---|---|
| 新任务默认开关开启 | PASS | PASS |
| 开关可切换并在重载后持久化 | PASS | DOM 状态正确 |
| 历史任务显示固定边界、单次识别 | PASS | PASS |
| 历史步骤编辑器两个可选开关均关闭 | PASS | PASS |
| 布局、遮挡与横向溢出 | PASS | PASS |
| 控制台 error/warn | 0 | 0 |

验证结束后已将新任务默认设置恢复为开启，取消历史步骤编辑而未保存任务配置，并停止本地验证服务。

## 当前验证命令

| 命令/检查 | 结果 |
|---|---|
| Issue #16 定向纯函数、Provider、诊断回归 | 72 passed |
| `uv run --project apps/sidecar --extra asr --extra dev pytest` | 159 passed；1 条既有 Starlette 弃用警告 |
| 定向 Ruff | PASS |
| Sidecar / tooling 两组 Ruff | PASS |
| Web `npm run lint` / `npm run build` | PASS |
| Cargo fmt / Windows MSVC locked check | PASS |
| `git diff --check` | PASS |
| 两条真实媒体 A/B | PASS |
| 应用内 Browser 桌面与移动端流程 | PASS |

## 待提交后门禁

- 独立 Reviewer 对精确提交 HEAD 复审；仅 `PASS` 后转 Ready。
- GitHub Actions 全绿后合并；随后快进本地 `main` 并删除本地/远端功能分支。
